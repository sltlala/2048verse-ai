后期优化默认关闭: 40 局对照显示无显著差异, 且有值饱和风险

A/B (ab-endgame.js, 每组 20 局, 固定深度 4):
  OFF 均值 112,658 ±10,864  中位 134,136  最低 27,164
  ON  均值 118,786 ±9,574   中位 132,240  最低 56,712
均值差远小于标准误 -> 不显著; 中位数几乎相同。

理论风险: 关键期多个招式常都含致死分支, 大死局惩罚(1e9)取平均后全部趋近
同一巨负值, 招式差异被抹平, 关键期等于随机选 (值饱和)。nneonneo 原版只用
200,000 适度惩罚正是为此。

处理:
- ai.js: 新增 setEndgameMode(on|off), 默认 off (复原旧行为: eval - 4*W_LOST_PENALTY)
- run.js: 新增 --endgame on|off
- sim.js: --endgame=on|off 走同一预设
- README: 如实记录 A/B 数据、值饱和风险, 以及单局方差大、2 局不足以判定回归的教训

旧行为是实测跑到 669,900/32768 的配置, 故恢复为默认。
